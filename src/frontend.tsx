/**
 * Frontend single entry.
 *
 * Most navigation + auth flow should be adjusted here so newcomers know where to edit:
 * - Routes: /login, /signup, /forgot-password, /, /admin
 * - Post-login redirect & localStorage
 * - Guarding routes
 */

export { default } from './App';

